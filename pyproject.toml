[project]
name = "godovnikov"
version = "0.1.0"
description = "WebSocket Connection Manager для FastAPI"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard,standart]>=0.135.1",
    "uvicorn>=0.34.0",
    "websockets>=16.0",
]
